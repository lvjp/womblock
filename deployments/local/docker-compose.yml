---
name: womblock

services:
  backend:
    build:
      context: ../..
      dockerfile: ./build/Dockerfile
    restart: unless-stopped
    ports:
      - 8081:8080
    volumes:
      - ./config.yaml:/etc/womblock/config.yaml:ro
