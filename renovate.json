{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":disableRateLimiting"
  ],
  "postUpdateOptions": [
    "gomodTidy",
    "gomodUpdateImportPaths"
  ],
  "dependencyDashboard": false,
  "rebaseWhen": "behind-base-branch",
  "timezone": "Europe/Paris",
  "customManagers": [
    {
      "customType": "regex",
      "description": "GitHub Actions linting workflow",
      "managerFilePatterns": [
        "/^\\.github/workflows/linting\\.yml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.+) depName=(?<depName>.+)\\s+\\w+: \\s?(?<currentValue>.+)"
      ]
    },
    {
      "customType": "regex",
      "description": "cargo install from GitHub Actions workflow",
      "datasourceTemplate": "crate",
      "managerFilePatterns": [
        "/^\\.github/workflows/gh-pages\\.yml$/"
      ],
      "matchStrings": [
        "cargo binstall (?<depName>.+)@(?<currentValue>.+) # renovate dependency"
      ]
    }
  ],
  "packageRules": [
    {
      "matchManagers": [
        "gomod"
      ],
      "matchDepNames": [
        "go"
      ],
      "matchDepTypes": [
        "golang"
      ],
      "rangeStrategy": "bump"
    }
  ]
}